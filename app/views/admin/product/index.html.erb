<%= render"layouts/navbaradmin" %>
  <div class="page-header header-filter" data-parallax="true" style="background-image: url('/assets/background/admin.jpg')">
    <div class="container">
      <div class="row">
        <div class="col-md-10">
          <h1 class="title text-center">Liste des <%= @products.size %> produits on-line</h1>
                <a class="carousel-control-prev" href="#" role="button" data-slide="prev">
                  <i class="fas fa-caret-square-left" style="font-size:50px"></i>
                  <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#" role="button" data-slide="next">
		  <i class="fas fa-caret-square-right" style="font-size:50px"></i>
                  <span class="sr-only">Next</span>
                </a>
        </div>
      </div>
    </div>
  </div>

  <div class="main main-raised bg-dark">
    <div class="container">


  <div class="vertical">
    <%= link_to "  ", admin_root_path, class:"pull-left btn btn-danger btn-just-icon fas fa-arrow-left fa-2x" %>
<h2 class="text-danger"> Retour Dashboard</h2>
  </div>
<br>
<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th scope="col"><h3><i class="far fa-hand-point-right fa-2x"></i>Kite</h3></th>
      <th scope="col">modèle</th>
      <th scope="col">Date de création</th>
      <th scope="col">Dernière modification</th>
      <th scope="col">voir en ligne</th>
      <th scope="col">nombre de photos</th>
      <th scope="col">Photos</th>
    </tr>
  </thead>
  <tbody>
<%  @products.each_with_index do |t,index| %>
<% if t.category === "kite" %>
    <tr>
      <th scope="row">n° <%= t.id %></th>
<%if t.picture.size > 5 %>
      <td><i class="fas fa-check text-success"></i> <%= t.name %></td>
      <td><%= t.created_at %></td>
      <td><%= t.updated_at %></td>
      <td><%= link_to "voir", kite_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= t.picture.size %></td>
      <td><%= link_to "modifier", edit_admin_product_path(t), class:"btn-sm btn-primary" %></td>
<%else%>
      <td><i class="fas fa-exclamation-triangle text-warning"></i> <%= t.name %></td>
      <td><%= t.created_at %></td>
      <td><%= t.updated_at %></td>
      <td><%= link_to "voir", kite_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= t.picture.size %></td>
      <td><%= link_to "Ajouter", edit_admin_product_path(t), class:"btn-sm btn-warning" %></td>
<% end %>
    </tr>
<% end %>
<% end %>
  </tbody>

  <thead>
    <tr>
      <th scope="col"><h3><i class="far fa-hand-point-right fa-2x"></i> Board</h3></th>
      <th scope="col">modèle</th>
      <th scope="col">Date de création</th>
      <th scope="col">Dernière modification</th>
      <th scope="col">voir en ligne</th>
      <th scope="col">nombre de photos</th>
      <th scope="col">Photos</th>
    </tr>
  </thead>

<tbody>
<%  @products.each_with_index do |t,index| %>
<% if t.category === "board" %>
    <tr>
      <th scope="row">n° <%= t.id %></th>
<%if t.picture.size > 5 %>
      <td><i class="fas fa-check text-success"></i> <%= t.name %></td>
      <td><%= t.created_at %></td>
      <td><%= t.updated_at %></td>
      <td><%= link_to "voir", board_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= t.picture.size %></td>
      <td><%= link_to "modifier", edit_admin_product_path(t), class:"btn-sm btn-primary" %></td>
<%else%>
      <td><i class="fas fa-exclamation-triangle text-warning"></i> <%= t.name %></td>
      <td><%= t.created_at %></td>
      <td><%= t.updated_at %></td>
      <td><%= link_to "voir", board_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= t.picture.size %></td>
      <td><%= link_to "Ajouter", edit_admin_product_path(t), class:"btn-sm btn-warning" %></td>
<% end %>
    </tr>
<% end %>

<% end %>

  </tbody>

  <thead>
    <tr>
      <th scope="col"><h3><i class="far fa-hand-point-right fa-2x"></i> Surf Board</h3></th>
      <th scope="col">modèle</th>
      <th scope="col">Date de création</th>
      <th scope="col">Dernière modification</th>
      <th scope="col">voir en ligne</th>
      <th scope="col">nombre de photos</th>
      <th scope="col">Photos</th>
    </tr>
  </thead>

<tbody>
<%  @products.each_with_index do |t,index| %>
<% if t.category === "surfboard" %>
    <tr>
      <th scope="row">n° <%= t.id %></th>
<%if t.picture.size > 5 %>
      <td><i class="fas fa-check text-success"></i> <%= t.name %></td>
      <td><%= t.created_at %></td>
      <td><%= t.updated_at %></td>
      <td><%= link_to "voir", surfboard_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= t.picture.size %></td>
      <td><%= link_to "modifier", edit_admin_surfboard_path(t), class:"btn-sm btn-primary" %></td>
<%else%>
      <td><i class="fas fa-exclamation-triangle text-warning"></i> <%= t.name %></td>
      <td><%= t.created_at %></td>
      <td><%= t.updated_at %></td>
      <td><%= link_to "voir", surfboard_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= t.picture.size %></td>
      <td><%= link_to "Ajouter", edit_admin_surfboard_path(t), class:"btn-sm btn-warning" %></td>
<% end %>
    </tr>
<% end %>

<% end %>

  </tbody>

  <thead>
    <tr>
      <th scope="col"><h3><i class="far fa-hand-point-right fa-2x"></i> Barres</h3></th>
      <th scope="col">modèle</th>
      <th scope="col">Date de création</th>
      <th scope="col">Dernière modification</th>
      <th scope="col">voir en ligne</th>
      <th scope="col">nombre de photos</th>
      <th scope="col">Photos</th>
    </tr>
  </thead>

<tbody>
<%  @products.each_with_index do |t,index| %>
<% if t.category === "accessory" && t.domain === "bar" %>
    <tr>
      <th scope="row">n° <%= t.id %></th>
<%if t.picture.size > 5 %>
      <td><i class="fas fa-check text-success"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "modifier", edit_admin_accessory_path(t), class:"btn-sm btn-primary" %></td>
<%else%>
      <td><i class="fas fa-exclamation-triangle text-warning"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "Ajouter", edit_admin_accessory_path(t), class:"btn-sm btn-warning" %></td>
<% end %>
    </tr>
<% end %>

<% end %>

  </tbody>
  <thead>
    <tr>
      <th scope="col"><h3><i class="far fa-hand-point-right fa-2x"></i> Pads / Straps</h3></th>
      <th scope="col">modèle</th>
      <th scope="col">Date de création</th>
      <th scope="col">Dernière modification</th>
      <th scope="col">voir en ligne</th>
      <th scope="col">nombre de photos</th>
      <th scope="col">Photos</th>
    </tr>
  </thead>

<tbody>
<%  @products.each_with_index do |t,index| %>
<% if t.category === "accessory" && t.domain === "pads" %>
    <tr>
      <th scope="row">n° <%= t.id %></th>
<%if t.picture.size > 5 %>
      <td><i class="fas fa-check text-success"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "modifier", edit_admin_accessory_path(t), class:"btn-sm btn-primary" %></td>
<%else%>
      <td><i class="fas fa-exclamation-triangle text-warning"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "Ajouter", edit_admin_accessory_path(t), class:"btn-sm btn-warning" %></td>
<% end %>
    </tr>
<% end %>

<% end %>

  </tbody>

  <thead>
    <tr>
      <th scope="col"><h3><i class="far fa-hand-point-right fa-2x"></i> Harnais</h3></th>
      <th scope="col">modèle</th>
      <th scope="col">Date de création</th>
      <th scope="col">Dernière modification</th>
      <th scope="col">voir en ligne</th>
      <th scope="col">nombre de photos</th>
      <th scope="col">Photos</th>
    </tr>
  </thead>

<tbody>
<%  @products.each_with_index do |t,index| %>
<% if t.category === "accessory" && t.domain === "harness" %>
    <tr>
      <th scope="row">n° <%= t.id %></th>
<%if t.picture.size > 5 %>
      <td><i class="fas fa-check text-success"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "modifier", edit_admin_accessory_path(t), class:"btn-sm btn-primary" %></td>
<%else%>
      <td><i class="fas fa-exclamation-triangle text-warning"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "Ajouter", edit_admin_accessory_path(t), class:"btn-sm btn-warning" %></td>
<% end %>
    </tr>
<% end %>

<% end %>

  </tbody>

  <thead>
    <tr>
      <th scope="col"><h3><i class="far fa-hand-point-right fa-2x"></i> Combinaisons</h3></th>
      <th scope="col">modèle</th>
      <th scope="col">Date de création</th>
      <th scope="col">Dernière modification</th>
      <th scope="col">voir en ligne</th>
      <th scope="col">nombre de photos</th>
      <th scope="col">Photos</th>
    </tr>
  </thead>

<tbody>
<%  @products.each_with_index do |t,index| %>
<% if t.category === "accessory" && t.domain === "wetsuit" %>
    <tr>
      <th scope="row">n° <%= t.id %></th>
<%if t.picture.size > 5 %>
      <td><i class="fas fa-check text-success"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "modifier", edit_admin_accessory_path(t), class:"btn-sm btn-primary" %></td>
<%else%>
      <td><i class="fas fa-exclamation-triangle text-warning"></i> <%= t.name %></td>
      <td><%= t.category %></td>
      <td><%= t.created_at %></td>
      <td><%= link_to "voir", accessory_path(t.id), class:"btn-sm btn-success" %></td>
      <td><%= link_to "Ajouter", edit_admin_accessory_path(t), class:"btn-sm btn-warning" %></td>
<% end %>
    </tr>
<% end %>

<% end %>

  </tbody>
</table>


</div>
</div>

